<%args>
$headers
</%args>
<%init>
return unless RT::BugTracker::Public->IsPublicUser;

# Protect email addresses
for my $h (@$headers) {
    next if $h->{'Tag'} =~ /^(?:Subject)$/i;
    $h->{'Value'} =~ s/@/ [...] /g;
}
</%init>
